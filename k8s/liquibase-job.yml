apiVersion: batch/v1
kind: Job
metadata:
  name: grabcar-postgre-sql
  namespace: grab-car  # Set the namespace
spec:
  template:
    spec:
      containers:
        - name: grabcar-auth-service
          image: ervincode/grabcar-postgre-sql:${IMAGE_TAG} # Uses the same image with Liquibase inside
          imagePullPolicy: Always
          command: ["mvn", "liquibase:update"]  # Runs migrations
          env:
            - name: ENV
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: SPRING_PROFILES_ACTIVE
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: DB_URL
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: DB_PASSWORD
#          volumeMounts:
#            - name: changelog-volume
#              mountPath: /app/resources/db/changelog
#      volumes:
#        - name: changelog-volume
#          emptyDir: {}
      restartPolicy: Never
